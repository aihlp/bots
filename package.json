{
  "name": "bot",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "npm run build:admin && npm run build:worker",
    "build:admin": "cd src/admin && npm ci && npm run build",
    "build:worker": "tsc --noEmit",
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240117.0",
    "typescript": "^5.9.3",
    "vitest": "^3.2.4",
    "wrangler": "4.101"
  },
  "dependencies": {
    "hono": "^4.0.0"
  }
}
